# ── Frontend (Vite) ──────────────────────────────────────────────────────
# PHP backend URL (defaults to internlink-api.cehpoint.co.in)
# VITE_PHP_API_URL=https://internlink-api.cehpoint.co.in/index.php

# Set to "development" to use localhost backend
# VITE_ENV=development
